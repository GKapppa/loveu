package com.loveu.loveu.model;

// Creamos el ENUM de reporte para así poder tener solamente 3 estados en nuestro reporte y no tener que preocuparnos por errores de tipeo o estados no validos.
public enum EstadoReporte {EN_REVISION, RESUELTO, RECHAZADO}
