CREATE TABLE matches (
    id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    perfilaid INTEGER NOT NULL,
    perfilbid INTEGER NOT NULL,
    status VARCHAR(50) NOT NULL DEFAULT 'ACTIVE',
    matched_at TIMESTAMP NOT NULL
);
